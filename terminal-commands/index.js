const terminalCommands = require('./terminal-commands');

terminalCommands.ls();
terminalCommands.touch('Cheese');
terminalCommands.mkdir('Sandwich');
terminalCommands.touch('Hot');
terminalCommands.mkdir('Sun');